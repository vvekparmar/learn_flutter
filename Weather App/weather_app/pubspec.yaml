name: weather_app
description: This Application demonstrate shows weather of city.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  flutter_spinkit: ^4.1.2
  http:
  geolocator: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

